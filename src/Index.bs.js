// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Js_exn from "rescript/lib/es6/js_exn.js";
import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Js_promise2 from "rescript/lib/es6/js_promise2.js";
import * as Libs from "@logseq/libs";
import * as Caml_js_exceptions from "rescript/lib/es6/caml_js_exceptions.js";
import * as LogseqSDK$LogseqBlockSpacer from "./LogseqSDK.bs.js";

var logseq = window.logseq;

var editor = logseq.Editor;

var logseqApp = logseq.App;

var date2JournalDay = (function (date) {
    const year = date.getFullYear().toString();
    const month = (date.getMonth() + 1).toString().padStart(2, '0');
    const day = date.getDate().toString().padStart(2, '0');

    const dateString = year + month + day;

    return Number(dateString);
  });

async function hasBuiltInProperty(block) {
  var properties = Belt_Option.mapWithDefaultU(Caml_option.null_to_opt(await logseq.Editor.getBlockProperties(block.uuid)), {}, (function (p) {
          return p;
        }));
  var includeBuiltInEditableProperty = (function (blockProperty) {
      return ["icon", "title", "tags", "template", "template-including-parent",
        "alias", "filters", "public", "exclude-from-graph-view"]
        .some(k => Object.prototype.hasOwnProperty.call(blockProperty, k))
    });
  return includeBuiltInEditableProperty(properties);
}

async function handleChildrenBlocks(childrens) {
  var insertContent = "";
  var len = childrens.length;
  if (len !== 1) {
    if (len !== 0) {
      var firstChildren = childrens[0];
      var secondChildren = childrens[1];
      var firstBlockIsEmpty = firstChildren.content === "";
      var secondBlockIsEmpty = secondChildren.content === "";
      if (firstBlockIsEmpty) {
        console.log("first children is empty, keep it, do nothing");
      } else if (await hasBuiltInProperty(firstChildren)) {
        if (secondBlockIsEmpty) {
          console.info("first children has built-in property, second children is empty, keep it,\
         do nothing");
        } else {
          console.info("first children has built-in property and second children are not empty, \
          insert a block after first children block");
          editor.insertBlock(firstChildren.uuid, insertContent, {
                sibling: true
              });
        }
      } else {
        console.info("first children is not empty and has no built-in property,\
           insert a block before this block");
        editor.insertBlock(firstChildren.uuid, insertContent, {
              before: true
            });
      }
      return ;
    }
    console.log("no children in current block/page");
    return ;
  }
  var oneChildren = childrens[0];
  if (oneChildren.content !== "") {
    console.log("There only one children and it is not empty, insert a block after this block");
    editor.insertBlock(oneChildren.uuid, insertContent, {
          before: true
        });
  }
  console.log("There only one children in current block/page: ", oneChildren);
}

async function getTodayJournalPageEntity(param) {
  var userConfig = await logseqApp.getUserConfigs();
  if (!userConfig.enabledJournals) {
    return ;
  }
  var currentGraph = await logseqApp.getCurrentGraph();
  var currentGraphUrl = currentGraph !== null ? currentGraph.url : (logseq.UI.showMsg("Can't get current graph, please open a graph first", "error", undefined), "");
  var allPages = Belt_Option.mapWithDefaultU(Caml_option.null_to_opt(await editor.getAllPages(currentGraphUrl)), [], (function (page) {
          return page;
        }));
  var journalPages = allPages.filter(function (page) {
        return page["journal?"];
      });
  var todayJournalDay = date2JournalDay(new Date());
  return Caml_option.undefined_to_opt(journalPages.find(function (journalPage) {
                  return Belt_Option.getExn(journalPage.journalDay) === todayJournalDay;
                }));
}

var cache = {
  contents: undefined
};

var todayJournalDay = {
  contents: undefined
};

logseqApp.onTodayJournalCreated(function (param) {
      todayJournalDay.contents = undefined;
    });

async function getTodayJournalPageEntityMemo(param) {
  if (Belt_Option.isSome(cache.contents) && Belt_Option.isSome(todayJournalDay.contents)) {
    return cache.contents;
  }
  var todayJournalPageEntity = await getTodayJournalPageEntity(undefined);
  cache.contents = todayJournalPageEntity;
  todayJournalDay.contents = date2JournalDay(new Date());
  return todayJournalPageEntity;
}

async function handleJournalPage(param) {
  var todayJournalPageEntity = await getTodayJournalPageEntityMemo(undefined);
  var todayJournalPageUuid = Belt_Option.mapWithDefaultU(todayJournalPageEntity, "", (function (page) {
          return page.uuid;
        }));
  var childrenBlocks = await editor.getPageBlocksTree(todayJournalPageUuid);
  handleChildrenBlocks(childrenBlocks);
}

async function handleNamedPage(param) {
  var blockEntity = Belt_Option.getExn(Caml_option.null_to_opt(await editor.getCurrentPage()));
  var blockEntity$1 = LogseqSDK$LogseqBlockSpacer.BlockOrPageEntity.classify(blockEntity);
  if (blockEntity$1.TAG === /* BlockEntity */0) {
    var block = Belt_Option.getExn(Caml_option.null_to_opt(await editor.getBlock(blockEntity$1._0.uuid, {
                  includeChildren: true
                })));
    return await handleChildrenBlocks(Belt_Option.mapWithDefaultU(block.children, [], (function (c) {
                      return c;
                    })));
  }
  var blocksTree = await editor.getPageBlocksTree(blockEntity$1._0.uuid);
  return await handleChildrenBlocks(blocksTree);
}

async function main(_baseInfo) {
  logseqApp.onRouteChanged(function (obj) {
        var template = obj.template;
        switch (template) {
          case "/" :
              handleJournalPage(undefined);
              return ;
          case "/page/:name" :
              handleNamedPage(undefined);
              return ;
          default:
            return ;
        }
      });
  handleJournalPage(undefined);
}

try {
  logseq.ready(function (baseInfo) {
        Js_promise2.$$catch(Js_promise2.then(main(baseInfo), (function (param) {
                    console.info("The plugin \"" + baseInfo.title + "\" which id is \"" + baseInfo.id + "\" has load");
                    return Promise.resolve(undefined);
                  })), (function (err) {
                console.error("Can't load the plugin " + baseInfo.title + " which id is \"" + baseInfo.id + "\"", err);
                return Promise.resolve(undefined);
              }));
      });
}
catch (raw_err){
  var err = Caml_js_exceptions.internalToOCamlException(raw_err);
  if (err.RE_EXN_ID === Js_exn.$$Error) {
    console.error(err._1);
  } else {
    throw err;
  }
}

export {
  
}
/*  Not a pure module */
